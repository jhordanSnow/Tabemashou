@model IEnumerable<Tabemashou_User.Models.Check>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Balance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Balance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCheck }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCheck }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCheck })
        </td>
    </tr>
}

</table>
